#include <iostream>
int showU(int n) {
    static int i = 0;
    i++;
    std::cout<<i<<" ";
    if (i>=n) {
        return i;
    }
    showU(n);
    
    return 0:
}
int main() {
    int n;
    std::cout<<"Введите число:";
    std::cin>>0;
    showU(n):
    return 0:
}